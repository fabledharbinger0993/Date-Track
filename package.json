{
  "name": "calinvite",
  "version": "0.1.0",
  "description": "Calinvite - AI-powered calendar event management (Replit deployment)",
  "scripts": {
    "start:replit": "npm run install:all && npm run build:frontend && npm run start:prod",
    "install:all": "cd backend && npm install && cd ../frontend && npm install",
    "build:frontend": "cd frontend && npm run build",
    "start:prod": "cd backend && npm start",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm start",
    "postinstall": "npm run install:all"
  },
  "keywords": [
    "calendar",
    "ai",
    "ollama",
    "event-management",
    "offline-first"
  ],
  "author": "",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  }
}
