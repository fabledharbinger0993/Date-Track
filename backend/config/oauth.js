// OAuth configuration has been intentionally left as a placeholder while OAuth
// functionality returns 501 (Not Implemented). Reintroduce provider credentials
// (clientId, clientSecret, redirectUri, scopes) here when wiring OAuth into the
// authentication controllers/routes. All required environment variables are
// documented in .env.example.
